{
  "name": "list_files",
  "description": "Lists files in a directory without recursion",
  "inputSchema": {
    "type": "object",
    "properties": {
      "system": {
        "type": "string",
        "description": "The system identifier"
      },
      "path": {
        "type": "string",
        "description": "The directory to list the contents of"
      }
    },
    "required": [
      "system",
      "path"
    ]
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "openWorldHint": false
  }
}